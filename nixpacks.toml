[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -m streamlit run backend/streamlit_app/app.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
